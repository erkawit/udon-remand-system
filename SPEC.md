# สเปกระบบติดตามคำร้องขอฝากขัง (ทางจอภาพ) — ศาลจังหวัดอุดรธานี

เอกสารนี้สรุปทุกข้อกำหนด/การตัดสินใจออกแบบที่ได้ข้อสรุปแล้ว สำหรับส่งต่อให้ผู้พัฒนานำไปสร้างระบบจริง
เจ้าของระบบ: **ศาลจังหวัดอุดรธานี** (ไม่ใช่ตำรวจ — ตำรวจเป็นผู้ใช้งานภายนอกของระบบศาล)

---

## 1. ที่มา/วัตถุประสงค์

ตั้งแต่การฝากขังผู้ต้องหาครั้งที่ 2 เป็นต้นไป พนักงานสอบสวนต้องยื่นคำร้องขอฝากขังทางระบบอิเล็กทรอนิกส์
ล่วงหน้าตามระเบียบศาลจังหวัดอุดรธานีว่าด้วยการฝากขังทางจอภาพ พ.ศ. 2569 ระบบนี้สร้างขึ้นเพื่อ:

1. คำนวณและแจ้งเตือนวันครบกำหนด/วันต้องยื่นให้พนักงานสอบสวนของ 23 สถานีในจังหวัดอุดรธานี
2. เป็นช่องทางอัพโหลด/รับส่งไฟล์คำร้องระหว่างตำรวจกับศาล
3. บังคับกฎเกณฑ์ทางกฎหมาย (เพดานฝากขัง, เวลาตัดยื่น) โดยอัตโนมัติ กันความผิดพลาดของมนุษย์

**สิ่งที่ระบบไม่ได้ทำ:** ไม่ใช่ระบบพิจารณาคำร้อง ไม่ใช่ระบบยื่นฟ้อง/ออกหมายขัง — เป็นเพียง "สื่อกลาง" รับส่งไฟล์และแจ้งเตือนกำหนดเวลาเท่านั้น การพิจารณาสั่งฝากขังยังทำผ่านการประชุมทางจอภาพตามระเบียบข้อ 9 (นอกขอบเขตระบบนี้)

## 2. ฐานกฎหมาย

- **ป.วิ.อาญา มาตรา 87 วรรคหก**: เพดานฝากขัง 48 วัน (สูงสุด 4 ครั้ง) หรือ 84 วัน (สูงสุด 7 ครั้ง) แล้วแต่ฐานความผิด ครั้งละไม่เกิน 12 วัน
- **ระเบียบศาลจังหวัดอุดรธานีว่าด้วยการฝากขังทางจอภาพ พ.ศ. 2569**
  - **ข้อ 5**: ตั้งแต่ฝากขังครั้งที่ 2 เป็นต้นไป ต้องยื่นคำร้องล่วงหน้า **1 วันทำการ** ก่อนวันครบกำหนดฝากขังจริง (ถ้าวันครบกำหนดตรงวันหยุด ให้ถือวันทำการก่อนหน้าเป็นวันครบกำหนดจริงก่อน แล้วจึงถอยอีก 1 วันทำการ)
  - **ข้อ 6**: ยื่นทางระบบได้ไม่เกิน **16.00 น.** ของวันที่ต้องยื่น เลยเวลานี้ต้องนำคำร้องไปยื่นต่อศาลด้วยตนเองและรอจนกว่าศาลจะมีคำสั่ง — **ระบบต้องบล็อกการยื่นทางอิเล็กทรอนิกส์ทันทีที่เลยเวลานี้**
  - ข้อ 9: ศาลพิจารณาการฝากขังทางจอภาพวันละ 1 ครั้ง 9.00–10.00 น. (อยู่นอกขอบเขตของระบบนี้ แต่เป็นบริบทที่เกี่ยวข้อง)

ตารางอ้างอิงการคำนวณ "ครบกำหนดวันไหน ต้องยื่นวันไหน" มีอยู่ในไฟล์ `ตารางอธิบาย.xlsx` ที่ศาลจัดทำไว้ — ตรรกะในโค้ด (`lib/legalLogic.js`) ผ่านการทดสอบเทียบกับตารางนี้ครบทั้ง 7 กรณีแล้ว

## 3. ผู้ใช้งานและสิทธิ์

### บทบาทที่ 1: พนักงานสอบสวน (จากทั้ง 23 สถานีในจังหวัดอุดรธานี)
- เข้าระบบด้วยบัญชีรายบุคคล (ชื่อ + รหัสผ่าน) ที่**เจ้าหน้าที่ศาลเป็นผู้สร้างให้เท่านั้น** (ไม่มีการสมัครเองแบบเปิดสาธารณะ — ต้องผ่านการยืนยันตัวตนจากศาลก่อนเสมอ)
- **เห็นเฉพาะคดีที่ตัวเองรับผิดชอบเท่านั้น ห้ามเห็นคดีของพนักงานคนอื่นเด็ดขาด แม้จะอยู่สถานีเดียวกัน — ไม่มีข้อยกเว้น**
- รับเป็นเจ้าของคดีจาก "กล่องจดหมายของสถานี" (คำร้องฝากขังครั้งที่ 1 / ยฝ.-ฝ. ที่ศาลจับคู่กับสถานีไว้แล้ว — ดูรายละเอียดข้อ 5.1) — วันที่ยื่นฝากขังครั้งแรกจะเป็นวันที่รับ เริ่มนับที่ครั้งที่ 2 อัตโนมัติ
- อัพโหลดไฟล์ PDF คำร้องได้เฉพาะก่อนเวลา 16.00 น. ของวันที่ต้องยื่น (ตามข้อ 6)
- ไม่ต้องยุ่งเกี่ยวกับการกำหนดเพดานฝากขัง (เป็นหน้าที่เจ้าหน้าที่ศาล)

### บทบาทที่ 2: เจ้าหน้าที่ศาล (สร้างได้หลายบัญชี ทำหน้าที่ผู้ดูแลระบบร่วมกัน)
- บัญชีแยกต่างหากจากระบบบัญชีตำรวจโดยสิ้นเชิง — **สร้างได้หลายบัญชี** (ไม่จำกัดแค่ 1 คนแบบเดิม) รองรับกรณีมีเจ้าหน้าที่ศาลมากกว่า 1 คนผลัดกันทำงาน ทุกบัญชีมีสิทธิ์เท่ากันหมด ไม่มีระดับสูง-ต่ำ สร้าง/ลบ/รีเซ็ตรหัสผ่านบัญชีศาลด้วยกันเองได้จากหน้า "บัญชีผู้ใช้" (ต้องมีอย่างน้อย 1 บัญชีเสมอ ลบบัญชีสุดท้ายไม่ได้ กันไม่มีใครเข้าระบบศาลได้เลย)
  - **บัญชีแรกของระบบ**: ไม่มีบัญชีตั้งต้นฝังในโค้ด (ไม่มีรหัสผ่าน default เลย) — หน้า login ฝั่งศาลจะเช็คอัตโนมัติว่ายังไม่มีบัญชีเลยหรือไม่ ถ้าใช่จะสลับไปโชว์ฟอร์ม **"ตั้งค่าบัญชีแรกของระบบ"** แทนฟอร์ม login ปกติ ให้กรอกชื่อ+ตั้งรหัสผ่านสร้างบัญชีแรกได้เอง แล้วล็อกอินให้ทันที — ใช้ได้ครั้งเดียวตอนระบบว่างเปล่าจริงๆ เท่านั้น หลังจากนั้นบัญชีถัดไปต้องสร้างผ่านหน้า "บัญชีผู้ใช้" (ต้องล็อกอินก่อน) เท่านั้น
  - **ลืมรหัสผ่าน**: ระบบยังไม่มีระบบกู้คืนรหัสผ่านผ่านอีเมล/SMS อัตโนมัติ (ตาม TODO ข้อ 9) — ถ้ามีบัญชีศาลอื่นที่ยังเข้าระบบได้ ให้เพื่อนร่วมงานรีเซ็ตให้จากหน้า "บัญชีผู้ใช้" ได้เลย แต่ถ้าเหลือบัญชีเดียวแล้วลืมรหัสผ่าน **จะไม่มีใครช่วยรีเซ็ตให้ได้ในระบบเลย ต้องให้ฝ่ายไอทีศาลรีเซ็ตจากฐานข้อมูลโดยตรง** — จึงมีคำเตือนแนะนำให้สร้างบัญชีสำรองอย่างน้อย 2 บัญชีตั้งแต่ตอนติดตั้งระบบ ทั้งในฟอร์มตั้งค่าบัญชีแรกและในหน้า "บัญชีผู้ใช้" (โชว์เตือนถ้าเหลือบัญชีเดียว)
- สร้างชุดเลขคำร้องฝากขังครั้งที่ 1 (ยฝ./ฝ.) ล่วงหน้าเป็นช่วง แล้วจับคู่แต่ละเลขกับสถานีเจ้าของคดี (ดูข้อ 5.1)
- เห็นคำร้องจากทุกสถานีพร้อมกัน
- ดาวน์โหลดไฟล์คำร้อง → ต้อง**ดาวน์โหลดไฟล์จริงก่อน** ปุ่ม "รับเรื่อง" ถึงจะทำงาน (กันพลาดกดรับเรื่องทั้งที่ยังไม่ได้เปิดไฟล์)
- แก้ไขเพดานฝากขัง (48 หรือ 84 วัน) ได้ทุกเมื่อผ่านปุ่ม "แก้ไข" ข้างตัวเลขเพดานในการ์ดคำร้อง — **ทุกคดีเริ่มต้นที่เพดาน 84 วัน (7 ครั้ง) โดยอัตโนมัติเมื่อสร้างคดีใหม่** เจ้าหน้าที่ศาลเป็นผู้ลดเหลือ 48 วัน (4 ครั้ง) เองถ้าคดีนั้นเข้าเกณฑ์ฐานความผิดที่เบากว่า
  - ⚠️ **ความเสี่ยงที่ยอมรับแล้ว**: เดิมออกแบบให้ระบบ**บังคับ**ต้องเลือกเพดานก่อนรับเรื่องครั้งที่ 4 (กันลืมเด็ดขาด) แต่เปลี่ยนมาใช้ค่าเริ่มต้น 84 วันแทนตามคำขอ เพื่อความสะดวก — **ถ้าเจ้าหน้าที่ศาลลืมลดเพดานสำหรับคดีที่ควรเป็น 48 วัน ระบบจะไม่เตือนและปล่อยให้ยื่นถึงครั้งที่ 7 ได้โดยไม่บล็อก** เป็นความเสี่ยงที่รับทราบและยอมรับแล้วเพื่อแลกกับความสะดวก ไม่ใช่ข้อบกพร่องที่พลาดไป
- สร้าง/ลบ/รีเซ็ตรหัสผ่านบัญชีพนักงานสอบสวน
- โอนย้ายความเป็นเจ้าของคดีระหว่างพนักงาน (เช่น กรณีย้ายหน่วย/ลาออก และสถานีลืมแจ้งลบบัญชี)
- จัดการปฏิทินวันหยุดศาล (เพิ่ม/ลบ)

## 4. ตรรกะการคำนวณวันที่ (รายละเอียดเต็มอยู่ใน `lib/legalLogic.js` + เทสต์ 46 เคส)

```
rawDeadline    = วันที่เริ่มฝากขังครั้งแรก + จำนวนวันสะสมที่ใช้ไปแล้ว (ไม่ใช่ 12 × (ครั้งที่-1) ตายตัว)
legalDeadline  = rawDeadline ปรับเลื่อนมาวันทำการก่อนหน้า ถ้าตรงวันหยุด/เสาร์-อาทิตย์
filingDeadline = legalDeadline ถอยกลับอีก 1 วันทำการ (ข้อ 5)
```

> ⚠️ **ข้อสังเกตทางกฎหมาย (ตรวจสอบแล้ว แต่ศาลยืนยันนโยบายปฏิบัติต่างจากนี้)**: กฎหมายจริง (ป.วิ.อาญา ม.87 วรรคหก) บังคับ 2 อย่าง: (1) ครั้งละไม่เกิน 12 วัน (2) รวมกันไม่เกิน 48 หรือ 84 วัน — ตัวเลข "4 ครั้ง/7 ครั้ง" ที่ใช้กันทั่วไปเป็นผลลัพธ์ทางคณิตศาสตร์ (48÷12, 84÷12) โดยสมมติว่าทุกครั้งได้เต็ม 12 วัน ถ้าศาลให้บางครั้งไม่ครบ 12 วัน ในทางทฤษฎีจำนวนครั้งจริงอาจมากกว่า 4 หรือ 7 ได้ตราบใดที่วันสะสมยังไม่เกินเพดาน
>
> **นโยบายที่ยืนยันกับศาลแล้ว (ใช้จริงในระบบ)**: ให้ **ปิดคดีเมื่อครบจำนวนครั้ง (4 หรือ 7) โดยตรงเสมอ** ไม่ต้องเช็ควันสะสมเพิ่ม เหตุผล: เพราะทุกครั้งฝากขังได้ไม่เกิน 12 วันเป็นกฎตายตัวอยู่แล้ว ทำให้จำนวนครั้งจะถึงเพดานเสมอก่อนหรือพร้อมกับวันสะสม ไม่มีทางที่วันสะสมถึงเพดานก่อนจำนวนครั้ง การเช็คสองชั้นจึงซ้ำซ้อนไม่จำเป็น

**กลไกที่ใช้งานจริง**: ระบบเก็บ `cumulativeDays` (วันสะสมที่ใช้ไปแล้วจนถึงสิ้นสุดครั้งก่อนหน้า) ในตัวคดี — ตอนเจ้าหน้าที่ศาล "ยืนยันรับเรื่อง" แต่ละครั้ง มีตัวเลือก **"ศาลให้ไม่ครบ 12 วัน?"** (ออปชัน ไม่บังคับ ค่าเริ่มต้น 12 วัน) ให้กรอกจำนวนวันจริงที่ศาลอนุญาตครั้งนั้น ระบบใช้ค่านี้แค่**คำนวณวันครบกำหนดของครั้งถัดไปให้แม่นยำ** (ไม่ใช่ +12 วันตายตัว) แต่**การปิดคดียังคงตัดสินจากจำนวนครั้ง (4/7) เท่านั้น** (`lib/legalLogic.js`: `computeOccasionDeadlines`, `lib/caseEngine.js`: `receiveOccasion`)

⚠️ **บั๊กสำคัญที่เคยพบและแก้แล้ว (เรื่องอื่น)**: ห้ามใช้ `Date.toISOString()` ในการแปลงวันที่ เพราะเป็นเวลา UTC ไม่ใช่เวลาไทย จะทำให้คำนวณ "วันนี้" ผิดไปหนึ่งวันช่วงเที่ยงคืน–ตี 7 ของทุกวัน ต้องใช้ `getFullYear()/getMonth()/getDate()` แบบ local time เท่านั้น (ดูฟังก์ชัน `toISO()` ใน `legalLogic.js` เป็นตัวอย่างที่ถูกต้อง)

## 5. Flow การทำงานเต็ม

### 5.1 การขอเลขคำร้องฝากขังครั้งที่ 1 (ยฝ./ฝ.) — จุดเริ่มต้นของทุกคดี

1. **เจ้าหน้าที่ศาล** ลงข้อมูลฝากขังครั้งแรกเป็นชุด ผ่านกล่อง "เพิ่มคำร้องฝากขังครั้งที่ 1" ด้านบนหน้าต่างศาล — เลือกประเภท (**ยฝ.** = ยาเสพติด หรือ **ฝ.** = คดีทั่วไป) + เลขเริ่ม + เลขสิ้นสุด + ปี พ.ศ. (ค่าเริ่มต้น 2569) + **วันที่ฝากขังครั้งแรก** (บังคับกรอก ใช้ค่าเดียวกันทั้งชุด เพราะปกติเป็นคดีจากการไต่สวนวันเดียวกัน) → **ระบบสร้างเป็น "คดีจริง" ทันที** (ยังไม่มีสถานี/พนักงานผูกอยู่) และคำนวณวันครบกำหนด/วันต้องยื่นครั้งที่ 2 ให้เสร็จเรียบร้อย **ปรากฏในปฏิทิน/แดชบอร์ดของศาลทันทีโดยไม่ต้องรอจับคู่สถานีหรือรอพนักงานมารับก่อน**
2. **เจ้าหน้าที่ศาล** จับคู่แต่ละคดีกับสถานีเจ้าของคดี ผ่านตัวเลือก (dropdown) ในกล่องเดียวกัน (หรือจับคู่ตรงจากการ์ดคดีในมุมมองปฏิทิน/รายการก็ได้) — ยังไม่ระบุตัวพนักงานคนใดคนหนึ่ง แค่ทำให้คดีนั้นไปโผล่เป็น **"กล่องจดหมายของสถานี"**
3. **พนักงานสอบสวนคนใดก็ได้ที่ล็อกอินอยู่ที่สถานีนั้น** เห็นรายการ "คดีที่รอรับเป็นเจ้าของ" ร่วมกัน พร้อมวันครบกำหนดที่คำนวณไว้แล้ว (เหมือนกล่องจดหมายส่วนกลาง — ยังไม่ถือเป็นการละเมิดกฎ "ห้ามเห็นคดีคนอื่น" เพราะยังไม่มีใครเป็นเจ้าของคดีในขั้นนี้) กด **"รับเป็นเจ้าของคดี"** เพื่อจับจองคดีนั้นเป็นของตัวเอง
4. ทันทีที่รับเป็นเจ้าของ — คดีนี้จะปรากฏเฉพาะในทะเบียนคดีส่วนตัวของพนักงานคนนั้นเท่านั้นตั้งแต่นี้ไป (กลับมาอยู่ภายใต้กฎ "ห้ามเห็นคดีคนอื่น" ตามปกติ) วันที่ต่างๆ ไม่เปลี่ยนแปลง เพราะคำนวณไว้ตั้งแต่ขั้นตอนที่ 1 แล้ว

> **ความหมายของประเภทเลข (ยืนยันแล้ว)**: **ยฝ. = ฝากขังคดียาเสพติด**, **ฝ. = ฝากขังคดีอาญาทั่วไป** — เป็นป้ายกำกับสำหรับแยกประเภทคดีเท่านั้น ไม่มีผลต่อการคำนวณ
>
> ✅ **ยืนยันแล้ว**: ระยะเวลาก่อนถึงศาลครั้งแรกของคดียาเสพติดอาจนานกว่าคดีทั่วไป (ตาม พ.ร.บ.วิธีพิจารณาคดียาเสพติด) แต่เรื่องนี้**อยู่นอกขอบเขตระบบ** (เราเริ่มติดตามตั้งแต่ครั้งที่ 2) ส่วน**การฝากขังแต่ละครั้ง (ครั้งที่ 2 เป็นต้นไป) ใช้กฎเดียวกันหมด ไม่เกิน 12 วันต่อครั้ง ทุกประเภทคดี** ไม่ว่ายาเสพติดหรือคดีทั่วไป — **ตรรกะคำนวณปัจจุบันถูกต้องแล้ว ไม่ต้องแยกสูตรตามประเภท ยฝ./ฝ.**
>

### 5.2 Flow ตั้งแต่ครั้งที่ 2 เป็นต้นไป

```
คดีถูกสร้างจากการจับจองเลข (5.1) — เพดานเริ่มต้น 84 วัน (7 ครั้ง)
        ↓
ระบบคำนวณวันครบกำหนด/วันต้องยื่น แจ้งเตือนในแดชบอร์ด (ไม่มีอีเมล/LINE — เป็นความรับผิดชอบของพนักงานที่ต้องตรวจสอบเอง)
        ↓
พนักงานอัพโหลด PDF ก่อน 16.00 น. ของวันที่ต้องยื่น (เลยเวลานี้ระบบบล็อกอัพโหลด ต้องไปยื่นด้วยตนเอง)
        ↓
เจ้าหน้าที่ศาลเห็นคำร้องใหม่ → ดาวน์โหลดไฟล์ (บังคับ ก่อนรับเรื่องได้)
        ↓
กดยืนยันรับเรื่อง → ระบบบันทึกประวัติ + เดินหน้าไปครั้งถัดไป (หรือปิดคดีถ้าครบเพดานแล้ว)
        ↓
[เมื่อใดก็ได้] เจ้าหน้าที่ศาลกด "แก้ไข" ลดเพดานเหลือ 48 วัน (4 ครั้ง) เองถ้าคดีเข้าเกณฑ์ฐานความผิดที่เบากว่า
        ↓
คดีปิดแล้ว → นับถอยหลัง 60 วัน แล้วลบรายละเอียดคดีทิ้งอัตโนมัติ (เก็บแค่ทะเบียนหลักฐานสรุปไว้ถาวร — ไฟล์ PDF แต่ละไฟล์ถูกลบไปแล้วตั้งแต่ 12 วันหลังอัพโหลดตามข้อ 6 ด้านล่าง)
```

> **หมายเหตุการตัดสินใจ (Push Notification)**: เคยพิจารณาเพิ่ม Push Notification (แจ้งเตือนแบบป๊อปอัพของเครื่อง แม้ปิดเบราว์เซอร์ทิ้งหมด) แล้ว **ตัดสินใจไม่ทำ** เพื่อคงหลักการเดิมว่าพนักงานสอบสวนต้องรับผิดชอบตรวจสอบกำหนดเวลาด้วยตนเอง — ถ้าต้องการแจ้งเตือนนอกเว็บ ใช้ทาง "ดาวน์โหลด/สมัครรับปฏิทิน .ics" (ดูข้อ 8 ที่มี `lib/icsFeed.js`) แทน

### 5.3 กรณีอัพโหลดไฟล์ผิดคดี/ผิดเวอร์ชัน

**ตัดสินใจแล้ว**: ไม่ต้องมีระบบ undo/audit ซับซ้อน — แค่เปิดโอกาสให้พนักงานอัพโหลดไฟล์ทับของเดิมได้เสมอ ตราบใดที่ยังไม่เลยเวลาตัด 16.00 น. ของวันที่ต้องยื่น (ข้อ 6) และคดียังไม่ปิด (`lib/caseEngine.js`: `uploadFile`, `canUploadFile`)

- ถ้าอัพทับได้ทัน: ไฟล์เก่าถูกแทนที่ด้วยไฟล์ใหม่ทันที ไม่มีการเก็บ log ไฟล์เก่าไว้แยกต่างหาก
- **จุดกันพลาดสำคัญ**: ทุกครั้งที่อัพโหลด (ครั้งแรกหรือทับของเดิม) ระบบรีเซ็ตสถานะ `downloaded` กลับเป็น `false` เสมอ — เผื่อกรณีเจ้าหน้าที่ศาลดาวน์โหลดไฟล์ผิดไปแล้วก่อนพนักงานจะทันแก้ไข พอพนักงานอัพไฟล์ที่ถูกต้องทับ ศาลจะต้องดาวน์โหลดไฟล์ใหม่อีกรอบก่อนกดปุ่ม "รับเรื่อง" ได้เสมอ กันไม่ให้เผลอรับเรื่องด้วยไฟล์เก่าที่ผิด
- ถ้าเลยเวลาตัดไปแล้ว (หรือคดีปิดไปแล้ว): อัพทับไม่ได้ ต้องไปยื่นด้วยตนเองที่ศาลตามข้อ 6 เหมือนกรณีทั่วไปที่เลยเวลา — ไม่มีข้อยกเว้นพิเศษสำหรับกรณีนี้
- **ไม่ครอบคลุมกรณีที่ศาลกด "รับเรื่อง" (`receiveOccasion`) ไปแล้ว** เพราะหลังรับเรื่องไปแล้วระบบเคลียร์ `fileName`/`downloaded` ของครั้งนั้นเป็นค่าว่างสำหรับครั้งถัดไปอยู่แล้วโดยธรรมชาติ ไม่มีทาง "อัพทับ" ไฟล์ของครั้งที่รับเรื่องไปแล้วได้อีก — ถ้าเกิดกรณีนี้ขึ้นจริง (รับเรื่องด้วยไฟล์ผิดไปแล้ว) เป็นเรื่องนอกขอบเขตที่ตกลงกันไว้ ต้องแก้ไขผ่านกระบวนการนอกระบบ (ติดต่อเจ้าหน้าที่ศาลโดยตรง) ไม่ใช่ผ่านฟังก์ชัน undo ในระบบ

**เงื่อนไขไฟล์ที่รับอัพโหลด (ยืนยันแล้ว)** — ตรวจก่อนแม้แต่จะเช็คเรื่องเวลา/สถานะคดี (`lib/legalLogic.js`: `validateUploadFile`):
- **นามสกุลไฟล์**: รับเฉพาะ **`.pdf`** เท่านั้น (ไม่สนตัวพิมพ์เล็ก/ใหญ่)
- **ขนาดไฟล์**: ไม่เกิน **20 MB** ต่อไฟล์ — เผื่อคดีที่มีเอกสารแนบเยอะ (บันทึกจับกุม/รายงานสอบสวน) แต่ยังกันไฟล์ผิดประเภทที่หลุดเข้ามาได้ ถ้าเกินต้องบีบอัดหรือแยกเป็นหลายไฟล์แนบเอง
- ต้องตรวจสอบทั้งฝั่ง **client** (ก่อนอัพโหลดจริง ประหยัด bandwidth ให้ผู้ใช้เห็น error ทันที) และฝั่ง **server** อีกชั้นเสมอ (กันกรณีมีคนข้าม client validation)
- `uploadFile` คืนค่าเป็น `{ case, ok, reason }` เสมอ — `reason` เป็นข้อความพร้อมใช้แสดงให้ผู้ใช้เห็นตรงๆ ได้เลย ไม่ต้อง map error code เอง

**ฝั่งเจ้าหน้าที่ศาลแจ้งไฟล์ผิด (ตัดสินใจแล้ว)** — กรณีเจ้าหน้าที่ศาลดาวน์โหลด/ตรวจดูไฟล์แล้วพบว่าผิดคดี (`lib/caseEngine.js`: `flagWrongFile`):
- มีปุ่ม **"แจ้งไฟล์ผิด/ให้อัพโหลดใหม่"** ให้เจ้าหน้าที่ศาลกดพร้อมระบุเหตุผล — ตั้งค่า `courtFlag: { reason, flaggedAt }` บนคดีนั้น
- **ไม่ลบไฟล์เดิมทันที** เก็บไว้ก่อนเผื่อเทียบภายหลัง — ไฟล์เดิมจะถูกแทนที่ก็ต่อเมื่อพนักงานอัพโหลดไฟล์ใหม่สำเร็จเท่านั้น (ตอนนั้น `courtFlag` จะถูกเคลียร์กลับเป็น `null` อัตโนมัติในตัว `uploadFile` เพราะถือว่าพนักงานแก้ไขแล้ว)
- **ระหว่างที่ `courtFlag` ยังค้างอยู่ ห้ามกด "รับเรื่อง" (`receiveOccasion`)** แม้ไฟล์จะมีสถานะ `downloaded: true` อยู่ก็ตาม — กันเจ้าหน้าที่ศาลเผลอรับเรื่องด้วยไฟล์ที่ตัวเองเพิ่งแจ้งว่าผิด (เช็คซ้ำที่ฝั่ง logic ไม่ใช่แค่ซ่อนปุ่มฝั่ง UI เท่านั้น)
- **เป็นการแจ้งเตือนในระบบเท่านั้น** (พนักงานเห็นตอนล็อกอินเข้ามาดูคดีของตัวเอง) ไม่ใช่การส่ง Email/SMS ออกไปจริง เพราะยังไม่ได้ต่อระบบแจ้งเตือนภายนอกตาม TODO ที่ค้างอยู่

### 5.4 กรณีเจ้าหน้าที่ศาลจับคู่คดีกับสถานีผิด

**ตัดสินใจแล้ว (เพิ่มเติม)**: มี 2 ทางแก้ไข แยกตามว่ามีพนักงานสอบสวนรับเป็นเจ้าของคดีไปแล้วหรือยัง —

**(ก) ก่อนตำรวจรับจับคู่ (ยังไม่มีพนักงานคนไหนรับเป็นเจ้าของคดี)** — เจ้าหน้าที่ศาล**แก้ไขได้เองทันทีในหน้าจอ** ไม่ต้องรอฝั่งตำรวจ: ที่รายการ "จับคู่สถานีแล้ว รอพนักงานสอบสวนมารับเป็นเจ้าของคดี" มีปุ่ม **"แก้ไข"** ต่อท้ายแต่ละคดี กดแล้วเลือกสถานีใหม่จากดรอปดาวน์ได้เลย (เรียก `assignCaseStation` ซ้ำตรงๆ ฟังก์ชันนี้ไม่มีเงื่อนไขห้ามเรียกซ้ำอยู่แล้ว) — เหมาะกับกรณีจับคู่ผิดที่สังเกตเห็นเร็ว ไม่ต้องรอให้ตำรวจมาเจอเอง

**(ข) หลังตำรวจรับจับคู่ไปแล้ว (มีพนักงานเป็นเจ้าของคดีแล้ว)** — เจ้าหน้าที่ศาลแก้ไขเองไม่ได้อีกต่อไป (dropdown หายไปเป็นป้ายข้อความนิ่งตามเดิม) ต้องให้พนักงานสอบสวน**ตำรวจ**เป็นคนกดปุ่ม **"คืนสำนวน"** (`lib/caseEngine.js`: `returnToPool`) แทน ทำได้ทั้งตอนคดียังไม่มีใครรับเป็นเจ้าของ (อยู่ในกล่องจดหมายของสถานี) และตอนที่รับเป็นเจ้าของไปแล้ว — **ไม่บังคับให้กรอกเหตุผล** กดแล้วมีแค่หน้ายืนยัน "ใช่/ไม่ใช่" ครั้งเดียวจบ (เดิมเคยมีช่องกรอกเหตุผลบังคับ แต่ตัดออกตามที่ผู้ใช้แจ้งว่าไม่จำเป็น ลดขั้นตอนการใช้งานจริง)

- คืนสำนวนแล้ว: เคลียร์ `station`, `officer`, `fileName`, `downloaded`, `courtFlag` กลับเป็นค่าว่างทั้งหมด — คดีกลับไปอยู่ในกอง "รอจับคู่สถานี" ของเจ้าหน้าที่ศาลใหม่ทันที
- บันทึก `returnedNote: { reason, returnedFromStation, returnedAt }` ติดไว้กับคดีเสมอ — ถ้าไม่ได้ส่ง `reason` มา (ปกติจะเป็นแบบนี้ เพราะไม่มีช่องกรอกในหน้าจอแล้ว) ใช้ข้อความอัตโนมัติ `"พนักงานสอบสวนแจ้งว่าไม่ใช่คดีของสถานีนี้"` แทน — เจ้าหน้าที่ศาลจะเห็นป้ายแดงเตือนตอนจับคู่สถานีใหม่ พร้อมสถานีเดิมที่เคยจับคู่ผิด กันจับคู่ผิดซ้ำที่เดิม
- **จำกัดเฉพาะคดีที่ยังไม่เคยถูกศาลรับเรื่องเลยสักครั้ง** (`history` ว่างเปล่า) เท่านั้น — ป้องกันไม่ให้การคืนสำนวนลบล้างประวัติการฝากขังจริงที่เกิดขึ้นไปแล้ว ถ้าคดีผ่านการรับเรื่องมาแล้วอย่างน้อยหนึ่งครั้ง ฟังก์ชันจะปฏิเสธและต้องแก้ไขผ่านเจ้าหน้าที่ศาลโดยตรงแทน (นอกขอบเขตที่ตกลงกันไว้) — เงื่อนไขนี้ยังบังคับอยู่เหมือนเดิม ไม่เกี่ยวกับการตัดช่องกรอกเหตุผลออก

**หมายเหตุ**: ทางเลือก (ก) เป็นแค่ UI แก้ไขได้เร็วขึ้นสำหรับกรณีจับคู่ผิดที่ยังไม่มีใครแตะต้อง — ไม่ได้แทนที่ทางเลือก (ข) เลย เพราะคนละสถานการณ์กัน (ก่อน vs หลังมีเจ้าของคดี)

### 5.5 ดาวน์โหลดไฟล์คำร้องแบบกลุ่ม (ฝั่งศาล)

**ตัดสินใจแล้ว (แก้ไข)**: ดาวน์โหลดแบบกลุ่มทำได้ **เฉพาะรายวัน + แยกทีละสถานีเท่านั้น** — เลือกวันที่ในปฏิทิน แล้วกดปุ่มดาวน์โหลดที่แถวของแต่ละสภ.แยกกัน โหลดเฉพาะไฟล์ของสถานีนั้นในวันนั้น (`StationSummaryTable`: ปุ่มดาวน์โหลดในแถวหัวข้อของแต่ละกลุ่มสถานี) — **เดิมเคยเป็นปุ่มเดียวโหลดรวมทุกสถานีพร้อมกัน แต่เปลี่ยนมาเป็นแยกทีละสถานีแล้วตามที่ผู้ใช้แจ้ง** เพราะใช้งานจริงสะดวกกว่า แยกไฟล์เป็นชุดตามสถานีชัดเจน ไม่ปนกัน

- **ยังคง** ไม่ทำดาวน์โหลดข้ามหลายวันพร้อมกัน — เจ้าหน้าที่ศาลดูทีละวันตามปฏิทินอยู่แล้วในการทำงานจริง
- เงื่อนไขไฟล์ที่จะถูกรวมในการดาวน์โหลดของแต่ละสถานี: `filingDeadline` ตรงกับวันที่เลือก + `station` ตรงกับสถานีนั้น + มี `fileName` (อัพโหลดแล้ว) + `!downloaded` (ยังไม่เคยดาวน์โหลด) — ไฟล์ที่โหลดไปแล้วจะไม่โหลดซ้ำ
- ดาวน์โหลดทีละไฟล์ห่างกัน 300ms ภายในสถานีเดียวกัน (ไม่พร้อมกันทีเดียว) เพื่อกันเบราว์เซอร์บล็อกการดาวน์โหลดหลายไฟล์รัวๆ
- ปุ่มดาวน์โหลดของแต่ละสถานีกดได้แม้ยังไม่ได้ขยาย (expand) ดูรายชื่อคดีในสถานีนั้น — เห็นจำนวนไฟล์พร้อมโหลดจากตัวเลขบนปุ่มได้เลย

### 5.6 จุดอ่อนด้านความถูกต้องของข้อมูล (ตรวจพบและแก้ไขแล้ว)

หลังทบทวนระบบทั้งชุด พบ 4 จุดที่เสี่ยงข้อมูลผิดเพี้ยนแบบเงียบๆ (ไม่มี error แต่ผลลัพธ์ไม่ถูกต้อง) — แก้ไขแล้วทั้งหมด:

**1. กันเลขคดีซ้ำ** — `createNumberBatch` เช็คช่วงเลขที่จะสร้างกับเลขคดีทั้งหมดที่มีอยู่แล้วในระบบ (ทั้งกำลังดำเนินการและปิดไปแล้ว) ก่อนสร้างเสมอ ถ้าเจอซ้ำแม้แต่เลขเดียว **ปฏิเสธสร้างทั้งชุดทันที** ไม่สร้างบางส่วน พร้อมบอกเลขที่ซ้ำให้ชัดเจน

**2. เจ้าของคดีต้องเป็นบัญชีจริงเท่านั้น** — ช่อง "โอนย้าย" เปลี่ยนจาก textbox พิมพ์อิสระ เป็น dropdown เลือกได้เฉพาะจากบัญชีที่มีอยู่จริงของสถานีนั้น (`accounts.filter(a => a.station === c.station)`) กันพิมพ์ชื่อผิดแล้วคดีหลุดไปอยู่ในชื่อที่ไม่มีบัญชีจริงรองรับ — ถ้าสถานีนั้นยังไม่มีบัญชีเลย จะแจ้งให้ไปสร้างบัญชีก่อน

**3. ลบบัญชีแล้วคดีคืนเป็น "ไม่มีเจ้าของ" อัตโนมัติ (ตัดสินใจแล้ว)** — `deleteAccount` ไม่บล็อกการลบ แต่จะเคลียร์ `officer` กลับเป็น `null` ให้กับคดีที่ยังไม่ปิดทุกคดีที่บัญชีนั้นเป็นเจ้าของอยู่ทันที (คืนกลับไปที่กล่องจดหมายของสถานีเดิม ให้พนักงานคนอื่นมารับต่อได้) — เหตุผล: พนักงานคนใหม่ที่มาแทนต้องขอบัญชีใหม่หรือใช้บัญชีเดิมอยู่แล้วตามขั้นตอนจริง ไม่จำเป็นต้องบังคับโอนย้ายก่อนลบ แต่ก็ยังมีหน้าจอเตือนก่อนลบเสมอ (โชว์จำนวนคดีที่จะได้รับผลกระทบ + ต้องกดยืนยันอีกครั้ง) คดีที่ปิดไปแล้วไม่ถูกแตะต้อง เก็บชื่อเจ้าของเดิมไว้เป็นประวัติตามปกติ

**4. ลดเพดานย้อนหลังไม่ให้ขัดแย้งกับครั้งปัจจุบัน** — ถ้าคดีอยู่ที่ครั้งที่เกิน 4 ไปแล้ว (เช่น ครั้งที่ 5-7) ตัวเลือก "4 ครั้ง (48 วัน)" จะ**หายไปจากหน้าจอเลย** เลือกไม่ได้อีกต่อไป (เหลือแค่ตัวเลือก 84 วัน) กันไม่ให้เกิดสถานะขัดแย้งกันเอง (ครั้งปัจจุบันเกินเพดานใหม่ทันที) — มีการเช็คซ้ำอีกชั้นที่ `updateCap` เองด้วย (ไม่ใช่แค่ซ่อนปุ่มฝั่ง UI) ถ้ามีทางเลี่ยงหน้าจอมาเรียกฟังก์ชันตรงๆ ก็ยังถูกปฏิเสธเงียบๆ อยู่ดี — เพิ่มเพดานกลับขึ้น (48→84) ยังทำได้อิสระเสมอเพราะไม่มีทางขัดแย้งกัน

**หมายเหตุ**: ทั้ง 4 จุดนี้เป็นตรรกะระดับ "จัดการบัญชี/เลขคดี" ของฝั่งศาล ไม่ใช่ตรรกะกฎหมายการคำนวณวันที่ จึงอยู่ใน `App` component ของ `demo/udon-remand-tracker.jsx` โดยตรง ยังไม่ได้แยกเข้า `lib/caseEngine.js` เหมือนฟังก์ชันอื่น — ตอนสร้าง Next.js จริงควรพิจารณาย้ายเข้าไปด้วยเพื่อทดสอบอัตโนมัติได้เหมือนกัน

## 6. นโยบายข้อมูล

- **ไฟล์ PDF แต่ละไฟล์ถูกลบอัตโนมัติ 12 วันหลังอัพโหลด** (`FILE_PURGE_DAYS` ใน `lib/legalLogic.js`) นับต่อไฟล์ ไม่เกี่ยวกับว่าคดีปิดหรือยัง — เดิมกำหนดไว้ 3 วัน แต่**แก้ไขแล้วเป็น 12 วัน** ตามข้อเสนอจากพนักงานสอบสวนจริงในภาคสนาม
  - ⚠️ **แก้ไขสมมติฐานเดิม**: ตอนกำหนด 3 วันครั้งแรก เคยประเมินว่า "ไม่มีความเสี่ยงจริง เพราะเจ้าหน้าที่ศาลดาวน์โหลดทุกวันทำการอยู่แล้ว" — **สมมติฐานนี้ผิดในทางปฏิบัติ**: พนักงานสอบสวนที่ยื่นคำร้องไว้ล่วงหน้าหลายวันก่อนวันครบกำหนดจริง (เช่น เตรียมไฟล์รอไว้ก่อนช่วงวันหยุดยาว) เจอไฟล์หมดอายุก่อนถึงวันที่ศาลต้องดาวน์โหลดจริงตามกำหนดยื่น — 3 วันไม่พอกับรูปแบบการทำงานจริง จึงขยายเป็น 12 วัน (เท่ากับจำนวนวันสูงสุดต่อครั้งของการฝากขังพอดี เป็นความบังเอิญ ไม่ได้ผูกกับ ป.วิ.อาญา ม.87 โดยตรง)
  - เหตุผลที่ยังไม่ขยายไปเกินนี้: มีต้นฉบับกระดาษที่ศาลสั่งแล้วเป็นหลักฐานตัวจริงอยู่แล้ว ไฟล์อิเล็กทรอนิกส์เป็นแค่สำเนาใช้งาน ไม่จำเป็นต้องเก็บยาวเท่าเอกสารต้นฉบับ
  - ระบบยังคงมีสถานะ "ไฟล์หมดอายุ" + ปุ่มอัพโหลดใหม่ไว้เป็นเครื่องมือกันพลาดเสมอ ไม่ว่าจะตั้งไว้กี่วันก็ตาม
- **รายละเอียดคดี/ประวัติการยื่นแต่ละครั้ง (audit log)** ยังคงเป็นคนละนโยบายจากไฟล์ PDF — เดิมตกลงกันว่าเก็บ 60 วันหลังคดีปิดแล้วลบ แต่ยังไม่ได้ข้อสรุปสุดท้ายว่าจะคงระยะเวลานี้ไว้หรือเปลี่ยนตามไฟล์ด้วย **ต้องคุยกับศาลให้ชัดอีกครั้ง**
- ข้อมูลคดีอาญาเป็นข้อมูลอ่อนไหว ควรพิจารณาเรื่อง PDPA และเลือกโฮสต์/ฐานข้อมูลที่ region ใกล้ไทยที่สุด (เช่น Singapore) หากไม่มีตัวเลือกในประเทศ

## 7. สถาปัตยกรรมที่แนะนำ

```
React/Next.js App (โฮสต์บน Vercel)
        │
        ├──→ ฐานข้อมูล Postgres (คดี, บัญชี, ปฏิทินวันหยุด, ประวัติ, สิทธิ์การเข้าถึง)
        │     — เก็บ "ตรรกะ/กฎ" ทั้งหมด ไม่ใช่แค่ที่เก็บไฟล์
        │
        └──→ Google Drive API (เก็บเฉพาะไฟล์ PDF ผ่าน Service Account + Shared Drive ขององค์กรศาล)
              — ผู้ใช้ไม่ login Google เอง, สิทธิ์การเข้าถึงคุมผ่านฐานข้อมูล/แอปเท่านั้น
```

**สำคัญ**: การควบคุมว่าใครเห็นคดีไหนต้องเป็นหน้าที่ของแอป (เช็คจากฐานข้อมูล) เสมอ **ห้ามพึ่งระบบแชร์สิทธิ์ของ Google Drive โดยตรง** เพราะควบคุมละเอียดระดับ "รายคดี" แบบที่ต้องการไม่ได้

### แยก URL ระหว่างพนักงานสอบสวนกับเจ้าหน้าที่ศาล

> ⚠️ **สถานะ: จำลองพฤติกรรมไว้ในเดโมแล้วเท่าที่ทำได้ — แต่ URL จริงยังต้องสร้างตอนทำ Next.js** artifact ในแชทเป็นหน้าเว็บเดียวอยู่ URL คงที่ เปลี่ยน path จริงแบบ `/police`, `/court` ไม่ได้ จึง**จำลองด้วย query parameter แทน**: เติม `?portal=police` หรือ `?portal=court` ท้ายลิงก์ของ `demo/udon-remand-tracker.jsx` แล้วรีเฟรช จะข้ามหน้าเลือกพอร์ทัลไปเข้าฝั่งนั้นตรงๆ ทันที และล็อกเอาต์แล้วก็จะกลับไปหน้า login ฝั่งเดิม **ไม่มีทางเห็น/กดสลับไปอีกฝั่งจากในนั้นเลย** เหมือนพฤติกรรม URL แยกจริงทุกประการ (ดูฟังก์ชัน `getPortalFromURL()` ในไฟล์) — ตอนสร้าง Next.js จริง ให้เปลี่ยนจุดนี้เป็นอ่านจาก route จริงแทน (`/police/...`, `/court/...`) โครงสร้างโค้ดที่เหลือ (การแยกหน้าจอ, การแยก state, การไม่มีปุ่มข้ามฝั่ง) ใกล้เคียงของจริงแล้ว

**ตัดสินใจแล้ว**: ใช้ **URL แยกกัน** (เช่น `/police/...` กับ `/court/...`) แต่ใช้โค้ดเบส/ฐานข้อมูลเดียวกัน (ไม่ใช่แอปแยกกันคนละโปรเจกต์) — เหตุผล: แก้ตรรกะกฎหมายที่เดียว (`lib/caseEngine.js`) กระทบทั้งสองฝั่งพร้อมกันเสมอ ไม่เสี่ยงลืมอัพเดตฝั่งใดฝั่งหนึ่งเหมือนถ้าแยกเป็นคนละโปรเจกต์

**ข้อควรระวัง**: การแยก URL ช่วยเรื่อง UX (ไม่มีทางเห็น/กดข้ามไปหน้าอีกฝั่งโดยไม่ตั้งใจ) แต่ **ไม่ใช่ตัวป้องกันความปลอดภัยหลัก** — ต้องตรวจสิทธิ์ทุกครั้งที่ฝั่งเซิร์ฟเวอร์ (เช็ค session/token จริง) ไม่ใช่พึ่งแค่ "ไม่มีลิงก์ให้กด" เพราะถ้ามีคนพิมพ์ URL `/court/...` เองตรงๆ โดยไม่มีการเช็คสิทธิ์ฝั่งเซิร์ฟเวอร์ ก็ยังเข้าถึงได้อยู่ดี (จุดนี้ query param แบบในเดโมก็ป้องกันไม่ได้เหมือนกัน เป็นแค่จำลอง UX ไม่ใช่ระบบสิทธิ์จริง)

โครงสร้างโฟลเดอร์ที่แนะนำ:
```
app/
├── police/
│   ├── login/page.tsx
│   └── dashboard/page.tsx
├── court/
│   ├── login/page.tsx
│   └── dashboard/page.tsx
└── lib/
    └── caseEngine.js   ← ตรรกะกฎหมายชุดเดียวที่ทั้งสองฝั่งเรียกใช้ร่วมกัน
```

**หมายเหตุ**: ตัวเดโม `.jsx` ในแชท (`demo/udon-remand-tracker.jsx`) ยังคงมีปุ่มสลับบทบาทอยู่ เพราะเป็นไฟล์เดี่ยวสำหรับสาธิตทั้งสองฝั่งในที่เดียว **ไม่ใช่พฤติกรรมที่ต้องการในระบบจริง** — ตอนสร้าง Next.js จริงให้ทำตามโครงสร้าง URL แยกด้านบนแทน

### แนวทางการออกแบบหน้าตา (Design Tokens)

**ตัดสินใจแล้ว**: ธีมสีโทน "เอกสารราชการ/กระดาษเก่า" (สีน้ำตาลทอง-เขียวขี้ม้า-แดงตราประทับ บนพื้นกระดาษครีม) จับคู่กับฟอนต์ **Sarabun** (ฟอนต์ราชการไทยมาตรฐาน) ทั้งเว็บ — ค่าจริงที่ใช้อยู่ในโค้ด (`demo/udon-remand-tracker.jsx` และ `udon-remand-tracker.html` บรรทัดต้นๆ ของไฟล์):

```js
const ink        = "#1B2A41"; // ตัวหนังสือหลัก/หัวเรื่องเข้ม
const paper       = "#EAE7DC"; // พื้นหลังหน้าเว็บ
const paperCard    = "#F4F2E9"; // พื้นหลังการ์ด/กล่องเนื้อหา (สว่างกว่าพื้นหลังหลักเล็กน้อย)
const line        = "#C9C2AC"; // เส้นขอบ/เส้นแบ่งทั่วไป
const brass       = "#A8762E"; // เน้น/เตือนระดับกลาง (เช่น "ใกล้ครบกำหนด")
const brassBg      = "#F3E7D2"; // พื้นหลังคู่กับ brass
const sealRed      = "#9C3B2E"; // สีแดงตราประทับ ใช้กับสถานะอันตราย/เลยกำหนด/ปฏิเสธ
const sealRedBg     = "#F3DEDA";
const sealGreen     = "#3F6B4C"; // สถานะสำเร็จ/ปิดคดีแล้ว
const sealGreenBg    = "#DFE8DE";
const slate        = "#3A5A73"; // สีน้ำเงินเทา ใช้กับสถานะปกติ/ตำรวจ
const slateBg       = "#DFE6EA";

const serif = "'Sarabun', sans-serif"; // ใช้กับหัวเรื่อง/ชื่อคดี (Tailwind: font-serif ไม่ได้ใช้ — override ตรงด้วย fontFamily)
const sans  = "'Sarabun', sans-serif"; // ใช้กับเนื้อหาทั่วไป
```

**หลักการใช้สี**: สีคู่ `xxx` + `xxxBg` เสมอ (เช่น `sealRed`/`sealRedBg`) — ตัวเข้มใช้กับตัวหนังสือ/ไอคอน/เส้นขอบ ตัวอ่อนใช้กับพื้นหลังของป้ายสถานะเดียวกัน ไม่ผสมข้ามคู่ (ไม่เอา `sealRed` ไปคู่กับ `slateBg`)

**ฟอนต์**: ต้องเป็น Sarabun เท่านั้นทั้งระบบ ห้าม fallback ไปฟอนต์ระบบเฉยๆ เพราะจะดูหลุดธีม "เอกสารราชการ" ทันที (เจอปัญหานี้จริงตอนทดสอบ — ดูวิธีแก้ในข้อ 8 เรื่องฝังฟอนต์เป็น base64 แทนโหลดจาก Google Fonts)

**Layout**: การ์ดขอบมน (`rounded-lg`/`rounded-md`) พื้นหลังสีอ่อนกว่าเนื้อหาโดยรอบเล็กน้อย เส้นขอบบางสีน้ำตาลอ่อน (`line`) ไม่ใช้เงา (shadow) เลยทั้งระบบ — เรียบตามสไตล์เอกสารราชการ ไม่ใช่สไตล์แอปทันสมัยทั่วไป

## 8. โค้ดที่ส่งมอบในชุดนี้

```
udon-remand-system/
├── udon-remand-tracker.html       ← เดโมพร้อมรัน ไม่ต้อง build เลย เปิดไฟล์ตรงๆ หรือวางบน Vercel เป็น static site ได้ทันที — **ไม่พึ่ง network ภายนอกแม้แต่จุดเดียว** (React/ReactDOM/Babel/ไอคอน/ฟอนต์/Tailwind CSS ฝังในไฟล์หมด) ทดสอบจริงผ่าน headless browser แล้วว่ารันได้ไม่มี error
├── demo/
│   └── udon-remand-tracker.jsx   ← ตัวเดโม React แบบโต้ตอบได้ครบทุกฟีเจอร์ — **ไม่มีข้อมูลตัวอย่างค้างแล้ว** (คดี/บัญชีตำรวจเริ่มต้นว่างเปล่าทั้งคู่ ลงข้อมูลจริงเองผ่านหน้าจอ)
├── lib/
│   ├── legalLogic.js             ← ตรรกะกฎหมายล้วนๆ ทดสอบแล้ว 31 เคส
│   ├── legalLogic.test.js
│   ├── caseEngine.js             ← ชั้นต่อยอด: นำกฎหมายมาใช้กับคดีจริง (UI ควรเรียกใช้ชั้นนี้)
│   ├── caseEngine.test.js        ← เทสต์ 43 เคส รวม simulation คดีเต็มเพดาน 48/84 วัน, กรณีอัพโหลดผิดคดี, คืนสำนวน, ไฟล์หมดอายุ ฯลฯ
│   ├── icsFeed.js                ← ตัวสร้างไฟล์ปฏิทิน .ics (สำหรับ URL สมัครรับปฏิทินในอนาคต)
│   └── icsFeed.test.js           ← เทสต์ 8 เคส
├── package.json                   ← รัน `npm test` ได้เลย (82 เคสผ่านหมด)
└── README.md
```

**หมายเหตุสำคัญ**: `demo/udon-remand-tracker.jsx` เป็นไฟล์เดี่ยวที่รันในกล่องพรีวิวแชทเท่านั้น จึงมีสำเนาฟังก์ชันคำนวณวันที่ซ้ำอยู่กับ `lib/legalLogic.js` — **ได้ตรวจสอบแล้วว่าตรรกะเหมือนกันทุกประการ (รวมถึงบั๊ก timezone ที่แก้แล้วทั้งสองที่)** เมื่อเริ่มสร้างโปรเจกต์ Next.js จริง ให้ตัว UI import จาก `lib/caseEngine.js` โดยตรง แล้วลบสำเนาที่ซ้ำใน `.jsx` ทิ้งได้เลย ไม่ต้องเขียนตรรกะใหม่

**บัญชีเจ้าหน้าที่ศาลตั้งต้น**: ไม่มีบัญชี/รหัสผ่าน default ฝังในโค้ดอีกต่อไป — เปิดหน้าศาลครั้งแรกจะเจอฟอร์ม "ตั้งค่าบัญชีแรกของระบบ" ให้สร้างบัญชีเองได้เลย (ดูรายละเอียดในข้อ 3) ปลอดภัยกว่าการฝัง credential ตั้งต้นไว้ในซอร์สโค้ด

## 9. สิ่งที่ยังไม่ได้ทำ / ต้องตัดสินใจเพิ่มก่อนใช้งานจริง

- [ ] สร้างโปรเจกต์ Next.js จริง ต่อกับฐานข้อมูล Postgres จริง (ตอนนี้ข้อมูลเป็น in-memory state หายเมื่อรีเฟรชหน้า)
- [ ] ระบบยืนยันตัวตนจริง — **รหัสผ่านในเดโมเก็บเป็นข้อความธรรมดา ห้ามใช้แบบนี้ในระบบจริงเด็ดขาด ต้อง hash รหัสผ่านฝั่งเซิร์ฟเวอร์**
- [ ] เชื่อม Google Drive API ตามสถาปัตยกรรมข้อ 7 (ต้องขอ Service Account key + Shared Drive ID จากศาล)
- [ ] Deploy ขึ้น Vercel จริง เลือก region ฐานข้อมูล/ที่เก็บไฟล์ (แนะนำ Singapore ถ้าไม่มีตัวเลือกในไทย)
- [ ] ตัดสินใจเรื่องระยะเวลาเก็บ audit log ถาวร (ข้อ 6)
- [ ] ตัดสินใจเรื่องนโยบายหมดอายุรหัสผ่าน (ตอนนี้ไม่มีวันหมดอายุ)
- [ ] ทดสอบกับผู้ใช้งานจริงกลุ่มเล็กก่อนขยายเต็มรูปแบบ 23 สถานี

**จุดอ่อนที่ทบทวนแล้วแต่ยังไม่ได้แก้ (นอกเหนือจาก 4 จุดในข้อ 5.6 ที่แก้แล้ว)** — ควรพิจารณาก่อนใช้งานจริงเต็มรูปแบบ:
- [ ] **Race condition ตอนแย่งรับเป็นเจ้าของคดี** — `claimForMe` เช็คแบบ read-then-write ในเดโม (ใช้คนเดียว ไม่มีปัญหา) แต่ตอนมีหลาย user จริงพร้อมกัน ต้องใช้ DB transaction/unique constraint กันสองคนรับคดีเดียวกันพร้อมกันได้ — **ตัดสินใจแล้ว: ไม่ต้องแก้ตอนนี้** (ผู้ใช้ยืนยันแล้ว)
- [ ] **ไม่มีการล็อกบัญชีหลังพยายามรหัสผ่านผิดหลายครั้ง (rate limit/lockout)** — เสี่ยงโดน brute force ถ้าเอาพฤติกรรมนี้ไปใช้จริงตรงๆ — **ตัดสินใจแล้ว: ไม่ต้องแก้ตอนนี้** (ผู้ใช้ยืนยันแล้ว)
- [x] ~~ฝั่งศาลไม่มีช่องค้นหา/กรองคดีข้ามวัน~~ — **แก้ไขความเข้าใจผิด**: ตรวจซ้ำพบว่ามีอยู่แล้วในแท็บ "รายการ" (`GroupedQueueList`) — ค้นหาเลขคดี/สถานี/ชื่อพนักงาน + กรองตามสถานะได้ และค้นข้ามทุกวันที่อยู่แล้ว (กรองจาก `courtQueue` ทั้งหมด ไม่ผูกกับวันที่เลือกในปฏิทิน) ไม่ต้องเพิ่มอะไรเพิ่ม
- [x] ~~ไม่มี pagination~~ — **แก้แล้ว**: เพิ่มคอมโพเนนต์ `PaginatedCaseList` (แบบ "แสดงเพิ่ม" โหลดทีละ 20 รายการ) ใช้ใน 4 จุดที่คำร้องมีโอกาสกองรวมกันเยอะ — (1) แต่ละกลุ่มสถานะในแท็บ "รายการ" (2) รายชื่อคดีเมื่อขยายดูแต่ละสถานีในปฏิทิน (จุดเสี่ยงสุด — วันทำการสุดท้ายก่อนวันหยุดยาวจะมีคำร้องจากหลายวันถูกเลื่อนมารวมกันเป็นหลักร้อยได้ตามที่ผู้ใช้ระบุ) (3) ขยายดูแต่ละวันในบล็อก "สรุปรายวัน" ของแดชบอร์ด (4) ขยายดูรายการยื่นสะสม (ยฝ./ฝ.) ในแดชบอร์ด ซึ่งไม่มีขอบเขตวันที่เลย สะสมได้ไม่จำกัด
- [x] ~~บัญชีศาลมีแค่ 1 บัญชีใช้ร่วมกัน~~ — **แก้แล้ว**: สร้างบัญชีเจ้าหน้าที่ศาลเพิ่มได้หลายบัญชีจากหน้า "บัญชีผู้ใช้" (ดูข้อ 3) — **ยังไม่ครอบคลุมคือ**: แต่ละบัญชีล็อกอินแยกกันได้แล้วก็จริง แต่ประวัติการกระทำ (เช่น ใครกดรับเรื่อง/ใครลดเพดาน) ยังไม่ได้บันทึกชื่อผู้กระทำไว้ในระบบ — ถ้าต้องการ accountability ระดับ "ใครทำอะไรเมื่อไหร่" แบบเต็มรูปแบบ ต้องเพิ่ม audit log แยกต่างหาก (นอกขอบเขตที่ทำตอนนี้)

## 10. ติดต่อสอบถาม

เอกสารนี้สรุปจากการพูดคุยออกแบบระบบระหว่างเจ้าของระบบ (ศาลจังหวัดอุดรธานี) กับ Claude (Anthropic) หากมีคำถามเรื่องเหตุผลการออกแบบจุดใด สามารถดูคอมเมนต์ในโค้ดประกอบได้ — ทุกจุดสำคัญมีคอมเมนต์อ้างอิงเหตุผล/ข้อกฎหมายกำกับไว้แล้ว
