สวัสดีครับ นี่คือระบบติดตามคำร้องขอฝากขังทางจอภาพ สำหรับศาลจังหวัดอุดรธานี ที่ออกแบบคุยกับ AI (Claude) จนได้ข้อสรุปเรื่องกฎหมาย/ตรรกะ/UX ครบแล้ว ขอส่งต่อให้ช่วยพัฒนาเป็นระบบใช้งานจริงครับ

## เริ่มจากตรงไหน

**อ่าน `SPEC.md` ก่อนไฟล์อื่นเสมอ** — เป็นเอกสารสรุปทุกข้อกำหนด/การตัดสินใจที่คุยกับศาลจนได้ข้อสรุปแล้ว ครอบคลุม: ฐานกฎหมายที่ใช้อ้างอิง, สิทธิ์ผู้ใช้แต่ละบทบาท, flow การทำงานเต็มทุกจุด (รวมกรณีขอบ เช่น อัพโหลดไฟล์ผิดคดี/จับคู่สถานีผิด), สถาปัตยกรรมที่แนะนำ, และรายการสิ่งที่ยังไม่ได้ทำ/ต้องตัดสินใจเพิ่มก่อนใช้งานจริง (ข้อ 9 ของเอกสาร) — ไม่ต้องมาถามผมซ้ำ ทุกจุดมีคำตอบ/เหตุผลอยู่ในนั้นแล้ว

## มีอะไรให้บ้างในซิป

- **`SPEC.md`** ← อ่านก่อนเสมอ
- **`lib/`** — ตรรกะกฎหมายและตรรกะจัดการคดีล้วนๆ เขียนแยกจาก UI ทดสอบอัตโนมัติแล้ว **75 เคส** (`npm test`) ครอบคลุมการคำนวณวันครบกำหนด/วันต้องยื่น, เพดานฝากขัง, การอัพโหลด/ปฏิเสธไฟล์, กรณีคืนสำนวน ฯลฯ — **ควรต่อยอดจากไฟล์นี้โดยตรง ไม่ต้องเขียนตรรกะกฎหมายใหม่**
- **`udon-remand-tracker.html`** — เดโมพร้อมรัน **ไม่ต้อง build เลย** เปิดไฟล์ตรงๆ หรือลาก-วางเป็น static site บน Vercel ได้ทันที (ตั้งชื่อ `index.html`) — **ทดสอบจริงแล้วว่ารันได้ไม่มี error** (รันในเอนจิ้น JS จริง จำลองคลิกโต้ตอบทุกขั้นตอนหลัก: สร้างบัญชีศาล, สร้างชุดเลขคดี ฯลฯ ผ่านหมด) ใช้ดูพฤติกรรม/ตรรกะได้ตรงที่สุดตอนนี้
- **`demo/udon-remand-tracker.jsx`** — ซอร์สโค้ด React ต้นฉบับ (ES module ปกติ) หน้าตา/ฟีเจอร์เหมือน `.html` ทุกอย่าง แต่เป็นไฟล์ต้นทางที่ควรใช้เป็นจุดเริ่มต้นตอนสร้าง Next.js จริง (import จาก `lib/caseEngine.js` แทนสำเนาที่ซ้ำในไฟล์นี้) — **ไม่มีข้อมูลตัวอย่างค้างอยู่แล้ว** (คดี/บัญชีเริ่มต้นว่างเปล่าทั้งคู่ ทั้ง `.html` และ `.jsx`)
- **`README.md`** — อธิบายโครงสร้างไฟล์สั้นๆ

## สิ่งสำคัญที่ต้องรู้ก่อนเริ่ม

1. **ตัวเดโม `.jsx` ไม่ใช่ระบบจริง** — เป็นไฟล์เดี่ยวสาธิตพฤติกรรม/UI เท่านั้น ข้อมูลเก็บแบบ in-memory หายเมื่อรีเฟรชหน้า **ต้องสร้างโปรเจกต์ Next.js จริงต่อกับฐานข้อมูล Postgres** ตามโครงสร้างที่แนะนำใน SPEC.md ข้อ 7
2. **แยก URL ตำรวจ/ศาล (`/police/...`, `/court/...`) จำลองไว้ในเดโมแล้วด้วย query param** (`?portal=police` / `?portal=court`) — พฤติกรรม UX ใกล้เคียงของจริงที่สุดเท่าที่ทำได้ในไฟล์เดี่ยว (ไม่มีทางเห็น/กดสลับไปอีกฝั่งเลย) แต่**ยังต้องเปลี่ยนเป็น route จริงตอนสร้าง Next.js** อยู่ดี เพราะ query param ไม่ใช่ระบบสิทธิ์จริง ดูรายละเอียดใน SPEC.md หัวข้อ "แยก URL ระหว่างพนักงานสอบสวนกับเจ้าหน้าที่ศาล"
3. **รหัสผ่านในเดโมเก็บเป็นข้อความธรรมดา (plain text) ห้ามใช้แบบนี้ในระบบจริงเด็ดขาด** ต้อง hash ฝั่งเซิร์ฟเวอร์ก่อนเริ่มใช้งานจริง — ไม่มีรหัสผ่านตั้งต้นฝังในโค้ดแล้ว (เปิดหน้าศาลครั้งแรกจะเจอฟอร์ม "ตั้งค่าบัญชีแรกของระบบ" ให้สร้างเองได้เลย) แต่ตอน hash รหัสผ่านจริงต้องคิดเรื่อง flow นี้ด้วยว่าจะยังให้สร้างบัญชีแรกแบบ self-service ผ่านหน้าเว็บ หรือเปลี่ยนเป็นให้ IT ศาล provision ให้แทน
4. **ยังไม่ได้เชื่อม Google Drive API จริง** — ตอนนี้ปุ่มอัพโหลด/ดาวน์โหลดไฟล์เป็นการจำลอง (สร้างไฟล์ Blob ปลอมในเบราว์เซอร์) ต้องขอ Service Account key + Shared Drive ID จากศาลก่อนต่อจริง

## ลำดับที่แนะนำให้เริ่ม

ดูรายการเต็มพร้อมสถานะ (ทำแล้ว/ตัดสินใจไม่ทำ/ยังไม่ได้ทำ) ได้ที่ SPEC.md ข้อ 9 แต่โดยสรุปควรเริ่มตามนี้:

1. สร้างโปรเจกต์ Next.js จริง โดย import ตรรกะจาก `lib/caseEngine.js` ตรงๆ (อย่าเขียนใหม่)
2. ต่อฐานข้อมูล Postgres จริงแทน in-memory state
3. ทำระบบยืนยันตัวตนจริง (hash รหัสผ่าน)
4. แยก URL ตำรวจ/ศาลตามที่แนะนำในข้อ 7
5. เชื่อม Google Drive API
6. Deploy ขึ้น Vercel

มีคำถามเรื่องเหตุผลการออกแบบจุดไหน เช็คคอมเมนต์ในโค้ดได้เลยครับ — ทุกจุดสำคัญมีคอมเมนต์อ้างอิงเหตุผล/ข้อกฎหมายกำกับไว้แล้วทั้งใน `lib/` และ `demo/udon-remand-tracker.jsx`
